/*
 * Stand-in for Zephyr's <zephyr/bluetooth/conn.h>, for the Zephyr stack's
 * tests. See gatt.h for what these stubs are and are not.
 */
#ifndef DEFGEN_TEST_ZEPHYR_BLUETOOTH_CONN_H
#define DEFGEN_TEST_ZEPHYR_BLUETOOTH_CONN_H

/* Generated code only ever passes a `struct bt_conn *` through, so an
   incomplete type is all the tests need. */
struct bt_conn;

#endif /* DEFGEN_TEST_ZEPHYR_BLUETOOTH_CONN_H */
